\documentclass{article}

% Citation style: not yet negotiated with the researcher using this project.
% Before filing or citing real (non-toy) references, run the negotiation in
% .claude/CLAUDE.md's "Citation-format & field contract" section and set
% `style=` here to whatever is decided -- never leave it on biblatex's
% implicit `numeric` default by omission.
\usepackage[backend=biber]{biblatex}
\addbibresource{references.bib}

\begin{document}
The toy analysis script computed a value of \input{generated/spike_number.tex}.

Urban green space provision can be tracked with a dedicated monitoring tool \cite{vanherzeleMonitoringToolProvision2003a}.

\printbibliography

\end{document}
